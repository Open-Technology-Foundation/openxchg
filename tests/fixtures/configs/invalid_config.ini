# Invalid configuration file with errors

[General]
DEFAULT_BASE_CURRENCY=XYZ
DEFAULT_VERBOSE=5
DEFAULT_DATE=invalid-date
AUTO_UPDATE_CURRENCY_LIST=maybe
UPDATE_CURRENCIES=relative/path

[InvalidSection]
SOMETHING=value

badKeyFormat=value
